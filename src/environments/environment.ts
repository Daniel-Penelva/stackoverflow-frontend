export const environment = {
    production: false,
    apiUrl: 'http://localhost:8080/'
};
// Este arquivo é utilizado para definir variáveis de ambiente para o ambiente de desenvolvimento.
// O valor de `production` é definido como `false`, indicando que não está em produção.